import { expect, test } from '@playwright/test'
test('shows compatibility or starts the simulator', async ({ page }) => { const errors:string[]=[]; page.on('pageerror', error=>errors.push(error.message)); await page.goto('./'); await expect(page.locator('main')).toBeVisible(); await expect(page.getByText(/WebGPU|Starting simulator|Simulator wird/).or(page.getByLabel(/Choose image|Bild oder Video/)).first()).toBeVisible(); expect(errors).toEqual([]) })
test('narrow preview remains in document flow', async ({ page }) => { await page.setViewportSize({width:390,height:844}); await page.goto('./'); if (await page.locator('.preview').count()) { await expect(page.locator('.preview')).toHaveCSS('position','relative'); await expect(page.locator('.preview')).toHaveCSS('aspect-ratio','16 / 9') } })
